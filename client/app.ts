class TimeToCurl {
	public init() {
		this.setUpEvents();
		const socket = io();
	}

	private setUpEvents() {
		document.addEventListener("DOMContentLoaded", () => {
			
		});
	}
}

new TimeToCurl().init();